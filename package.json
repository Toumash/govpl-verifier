{
  "name": "gov-pl-verifier",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run generate-icons && vite build",
    "preview": "vite preview",
    "generate-icons": "node scripts/generate-icons.cjs"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.2.1",
    "sharp": "^0.34.5",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
